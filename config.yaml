# KTB Cash Optimization — Unity Catalog / SQL warehouse config
# Env vars override these defaults (used by Databricks Apps via app.yaml).
# Schema source of truth: database_table_blueprint.md (12 tables)

warehouse_id: "ef9767b2a2846fa7"   # Starter Warehouse
catalog: "mdp_dev_dit"
schema: "default"
business_date: "auto"   # auto = latest date in fact_cash_position
center_id: "006CC2"     # ศูนย์เงินสดขอนแก่น (was: region_code)
center_name: "Khon Kaen"
use_unity_catalog: true

# ─── Table paths in Unity Catalog (12 tables per blueprint) ─────────
tables:
  # Branch (real source ✔)
  dim_branch: "mdp_dev_dit.default.dim_branch"
  fact_cash_position: "mdp_dev_dit.default.fact_cash_position"
  fact_cash_flow_daily: "mdp_dev_dit.default.fact_cash_flow_daily"
  fact_branch_denomination: "mdp_dev_dit.default.fact_branch_denomination"
  # Machine (pending real source)
  dim_machine: "mdp_dev_dit.default.dim_machine"
  fact_machine_position: "mdp_dev_dit.default.fact_machine_position"
  fact_machine_denomination: "mdp_dev_dit.default.fact_machine_denomination"
  fact_machine_flow_daily: "mdp_dev_dit.default.fact_machine_flow_daily"
  # Route (pending real source)
  dim_truck: "mdp_dev_dit.default.dim_truck"
  fact_route_summary: "mdp_dev_dit.default.fact_route_summary"
  fact_route_stop: "mdp_dev_dit.default.fact_route_stop"
  fact_route_stop_denomination: "mdp_dev_dit.default.fact_route_stop_denomination"

# Optional overrides via environment:
#   V2_CATALOG / V2_SCHEMA / V2_WAREHOUSE_ID (used by uc_repo_v2.py)
#   DATABRICKS_WAREHOUSE_ID  (Apps: valueFrom sql-warehouse)
