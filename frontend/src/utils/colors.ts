/** Mirrors the status-color custom properties in styles/index.css — single source of truth, theme-aware. */
export const COLOR = {
  green: "var(--green)",
  red: "var(--red)",
  amber: "var(--amber)",
  accent: "var(--accent)",
  sky: "var(--sky)",
  purple: "var(--purple)",
  slate: "var(--slate)",
  orange: "var(--orange)",
  gold: "var(--gold)",
  blue: "var(--blue)",
} as const;
